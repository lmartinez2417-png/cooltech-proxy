{
  "name": "fixit-ai-proxy",
  "version": "1.0.0",
  "description": "FixIt AI proxy server — handles Anthropic API calls with rate limiting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "jest --forceExit --detectOpenHandles"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "firebase-admin": "^13.0.2",
    "helmet": "^7.1.0",
    "ioredis": "^5.4.1",
    "rate-limit-redis": "^4.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
